[alias]
ci-test = "nextest run --locked --no-tests=pass"
ci-fmt = "fmt --package k2tools -- --check"
ci-lint = "clippy --package k2tools --all-targets -- -D warnings -W clippy::pedantic -A clippy::doc_markdown"
